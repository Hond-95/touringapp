<% if user_signed_in? && @user != current_user%>
  <div id="follow_form">
    <% if current_user.following?(@user) %>
      <%= render "user/unfollow" %>
    <% else %>
      <%= render "user/follow" %>
    <% end %>
  </div>
<% end %>
<div id="follow_count">
  <%= render "user/stats" %>
</div>

