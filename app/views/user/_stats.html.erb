<div class="stats">
  <%= link_to following_user_path(user.id), class: "following_count" do %>
    <strong><%= user.following.count %></strong>
    フォロー中
  <% end %>
  <%= link_to followers_user_path(user.id), class: "follower_count" do %>
    <strong><%= user.followers.count %></strong>
    フォロワー
  <% end %>
</div>
